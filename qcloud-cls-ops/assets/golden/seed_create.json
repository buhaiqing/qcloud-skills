{
  "_seed": true,
  "_seed_generator": "generate_golden_seeds.py",
  "_seed_skill": "qcloud-cls-ops",
  "_seed_action_verb": "create",
  "intent": "Create a new resource with default settings in {{user.region}}",
  "input": {
    "action": "<REPLACE_WITH_REAL_TCCLI_ACTION_FOR_CREATE>",
    "region": "{{env.TENCENTCLOUD_REGION}}"
  },
  "expected": {
    "fixture": null,
    "assertions": [
      {
        "path": "$.Response",
        "op": "exists"
      }
    ]
  }
}
